syntax = "proto3";
package email_client;

import "inbox.proto";

message AuthenticateRequest {
    string username = 1;
    string password = 2;
}

message AuthenticateResponse {
    bool success = 1;
    repeated InboxHeader inboxes = 2;
}

